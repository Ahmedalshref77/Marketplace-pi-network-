import React from "react";


function ProductDetails({ product, onBack, onBuy }) {
  return (
    <div>
      <h2>تفاصيل المنتج</h2>
      <img src={product.image} alt={product.name} width="200px" />
      <h3>{product.name}</h3>
      <p>{product.description}</p>
      <p>السعر: {product.price} π</p>
      <button onClick={() => onBuy(product)}>شراء</button>
      <button onClick={onBack}>رجوع</button>
    </div>
  );
}


export default ProductDetails;