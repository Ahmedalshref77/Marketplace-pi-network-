import React from "react";


function Wallet({ balance, transactions }) {
  return (
    <div>
      <h2>💰 المحفظة</h2>
      <p>الرصيد الحالي: {balance} π</p>
      <h3>سجل العمليات:</h3>
      <ul>
        {transactions.map((t, i) => (
          <li key={i}>{t}</li>
        ))}
      </ul>
    </div>
  );
}


export default Wallet;